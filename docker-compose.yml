services:
  tg-bot:
    build: .
    container_name: kachbot
    restart: always
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
    volumes:
      - ./users.db:/app/users.db
    ports:
      - "3000:3000"